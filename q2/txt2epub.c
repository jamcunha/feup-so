#include <stdio.h>
#include <stdlib.h>

int main (int argc, char *argv[]) {
    /* check if arguments are present */
    if(argc < 2) {
        printf("usage: txt2epub file [file]\n");
        return EXIT_FAILURE;
    }

    /* se tiver so um ficheiro nao fazer fork mas fazer logo o comando */
    
    return EXIT_SUCCESS;
}

